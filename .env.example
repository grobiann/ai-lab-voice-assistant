# .env.example — API 키 설정 예시
# 이 파일을 .env 로 복사하고 실제 키를 입력하세요
# cp .env.example .env

# STT_MODE = "openai" 또는 "local+llm" 사용 시 필요
OPENAI_API_KEY=sk-...

# STT_MODE = "local+llm" 사용 시 필요
ANTHROPIC_API_KEY=sk-ant-...
